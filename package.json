{
  "name": "angle-mort",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "build": "npm run typecheck && vite build",
    "typecheck": "tsc --noEmit && tsc -p src/sim/tsconfig.json --noEmit && tsc -p src/planner/tsconfig.json --noEmit"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "@types/three": "^0.186.0",
    "fast-check": "^4.10.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
